all:
  children:
    ifive-dc:
      hosts:
        ifive-dc-01:
          ansible_host: "{{ ifive_dc_01_ip }}"
        ifive-dc-02:
          ansible_host: "{{ ifive_dc_02_ip }}"
      vars:
        ansible_connection: winrm
        ansible_winrm_transport: ntlm
        ansible_winrm_server_cert_validation: ignore
        ansible_port: 5986
        ansible_winrm_scheme: https


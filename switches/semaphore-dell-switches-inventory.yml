all:
  children:
    dell_switches:
      hosts:
        LAB-SWITCH-01:
          ansible_host: "{{ switch_01_ip }}"
        LAB-SWITCH-02:
          ansible_host: "{{ switch_02_ip }}"
      vars:
        ansible_connection: network_cli
        ansible_network_os: ios
        ansible_become: no
        ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

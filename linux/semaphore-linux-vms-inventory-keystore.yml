all:
  children:
    linux_vms:
      hosts:
        LAB-NA-ANSI:
          ansible_host: "{{ linux_vm_01_ip }}"
        LAB-NA-EPL:
          ansible_host: "{{ linux_vm_02_ip }}"
      vars:
        ansible_connection: ssh
        ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
